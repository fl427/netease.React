export {};
// class ImageCache {
//   cacheMap = new Map<string, string>();
//   updateCache(src: string, data: string): void {
//     this.cacheMap.set(src, data);
//   }
//   getCache(src: string): string | undefined {
//     return this.cacheMap.get(src);
//   }
// }
// export const imageCache = new ImageCache();
