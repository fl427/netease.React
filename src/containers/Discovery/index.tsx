import React from 'react';
import './index.scss';

const Discovery: React.FC = () => {
    return (
        <div className={'discovery'}>
            Discovery
        </div>
    )
};

export default Discovery;